name: list_view_with_data_source
description: "Build ListView via DataSource that separated from UI"
version: 1.0.0
repository: https://github.com/ichiko/list_view_with_data_source

environment:
  sdk: '>=3.2.0 <4.0.0'
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
